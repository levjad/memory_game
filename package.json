{
  "name": "memory_game",
  "version": "1.0.0",
  "description": "Simple Memory Game using Vanilla JavaScript",
  "main": "game-logic.js",
  "scripts": {
    "start": "parcel index.html --out-dir tmp --open",
    "build": "rm -rf .cache dist tmp && parcel build index.html --no-source-maps --public-url ./",
    "delete": "rm -rf .cache dist tmp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  }
}
